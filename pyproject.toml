[project]
name = "biomedical-ai-mcp"
version = "0.1.0"
description = "Biomedical Research Assistant and MCP Server with local evidence retrieval"
readme = "README.md"
requires-python = ">=3.14,<3.15"
license = { file = "LICENSE" }
authors = [
    { name = "shaiksameer667ss-prog" }
]
dependencies = [
    "mcp==2.1.1",
    "pypdf==6.17.0",
]

[tool.uv]
package = false