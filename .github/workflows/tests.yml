name: Tests

on:
  push:
    branches:
      - main
  pull_request:
name: Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          version: "0.12.10"

      - name: Install locked dependencies
        run: uv sync --locked

      - name: Initialize test database
        run: uv run python database.py

      - name: Run test suite
        run: uv run python run_all_tests.py
        run: uv run python run_all_tests.py